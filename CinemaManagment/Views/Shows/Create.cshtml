@model CinemaManagment.Models.Show

@{
    ViewData["Title"] = "Create";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Create</h1>

<h4>Show</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="ShowStarts" class="control-label"></label>
                <input asp-for="ShowStarts" class="form-control" />
                <span asp-validation-for="ShowStarts" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="MovieTitle" class="control-label"></label>
                <select asp-for="MovieTitle" class ="form-control" asp-items="ViewBag.MovieTitle"></select>
            </div>
            <div class="form-group">
                <label asp-for="CinemaHallId" class="control-label"></label>
                <select asp-for="CinemaHallId" class ="form-control" asp-items="ViewBag.CinemaHallId"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
